<template>
  <section class="bg-primary-50">
    <div class="mx-auto max-w-8xl p-spacing py-10 lg:py-10 xl:py-20">
      <div class="flex flex-col items-start md:items-center md:flex-row gap-8 xl:gap-28 text-secondary-700">
        <div v-if="data.image_position === 'LEFT'" class="order-first md:order-none basis-1/2">
          <img :src="data.image?.url || ''" alt="Feature image" width="800" height="300" class="rounded-2xl w-full aspect-[29/19] object-cover" />
        </div>
        <div class="basis-1/2">
          <h2 class="text-mb-4xl lg:text-5xl font-medium text-secondary-700 text-balance">
            {{ data.heading }}
          </h2>
          <p class="text-lg mt-4 xl:mt-6 font-medium text-wrap text-balance">
            {{ data.sub_heading }}
          </p>
          <router-link to="#">
            <Button>
              {{ data.button.text }}
            </Button>
          </router-link>
        </div>
        <div v-if="data.image_position === 'RIGHT'" class="basis-1/2">
          <img :src="data.image.url || ''" alt="Feature image" width="800" height="300" class="rounded-2xl w-full aspect-[29/19] object-cover" />
        </div>
      </div>
    </div>
  </section>
</template>

<!-- Add a script to fetch the data -->
<script setup>

import { computed } from "vue";

// Pass the data as props to the page
const props = defineProps(["data"]);
const data = computed(() => props.data);

</script>